import React, { FunctionComponent } from 'react'

const Header: FunctionComponent = () => (
  <header className="sticky top-0 border-b p-4 bg-gray-50">
    <h1 className="text-3xl">Forex Dashboard</h1>
  </header>
)

export default Header
